apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service  # must match labels on your pod/deployment
  ports:
    - port: 80          # the port exposed by the service
      targetPort: 80  # the port your container listens on